<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LuteForAndroidv2" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/lute_primary</item>
        <item name="colorPrimaryVariant">@color/lute_primary_variant</item>
        <item name="colorOnPrimary">@color/lute_on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/lute_secondary</item>
        <item name="colorSecondaryVariant">@color/lute_secondary</item>
        <item name="colorOnSecondary">@color/lute_on_primary</item>
        <!-- Background colors -->
        <item name="android:colorBackground">@color/lute_background</item>
        <item name="colorOnBackground">@color/lute_on_background</item>
        <item name="colorSurface">@color/lute_surface</item>
        <item name="colorOnSurface">@color/lute_on_surface</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/lute_primary_dark</item>
        <!-- Customize your theme here. -->
    </style>
</resources>